{
  "name": "terrain-ts",
  "version": "1.0.0",
  "description": "TypeScript port of SimGolf's Terrain.dll — tile-based terrain engine with WebGL rendering",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "demo": "vite demo/"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "vite": "^6"
  }
}
